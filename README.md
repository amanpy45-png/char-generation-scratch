# char-generation-scratch
A minimal character-level RNN built from scratch using NumPy. It demonstrates one-hot encoding, forward pass, backpropagation through time (BPTT), and gradient descent. Trained on a tiny dataset ("hello") to predict next characters, making it ideal for learning sequence modeling fundamentals.
